package com.springer.knakobrak.net.messages;

public class LoadingCompleteMessage extends NetMessage {
    // Sent from the server when it has sent all needed data to the clients

    public LoadingCompleteMessage() {
    }
}
